import { css } from '@emotion/react';

const container = css`
  display: flex;
  justify-content: center;
  max-height: 100vh;
  max-width: 100vw;
  padding: 20px;
`;

const contentContainer = css`
  max-width: 80vw;
`;

const styles = {
  container,
  contentContainer
};

export default styles;
