import { css } from '@emotion/react';

const container = css`
  display: flex;
  justify-content: center;
  max-height: 100vh;
  max-width: 100vw;
  padding: 20px;
`;

const styles = {
  container
};

export default styles;
