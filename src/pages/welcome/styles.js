import { css } from '@emotion/react';

const container = css``;

const button = css`
  background: none;
  border: none;
`;

const header = css`
  align-items: flex-end;
  display: flex;
  justify-content: end;
  padding: 0 8px;
`;

const styles = {
  container,
  button,
  header
};

export default styles;
