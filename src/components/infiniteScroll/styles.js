import { css } from '@emotion/react';

const wrapper = css`
  background-attachment: fixed;
`;

const bottom = css`
  text-align: center;
`;

const styles = {
  wrapper,
  bottom,
};

export default styles;
