import { css } from '@emotion/react';

const empty = css`
  font-size: 32px;
  font-weight: bold;
  text-align: center;
`;

const bottomWrapper = css`
  margin-bottom: 30px;
`;

const styles = {
  empty,
  bottomWrapper
};

export default styles;
